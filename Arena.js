export default class Arena {
    constructor(width, height) {
        const matrix = [];

        this.matrix = matrix;
    }

    clear() {
        this.matrix.forEach(row => row.fill(0));
    }

    merge(player) {

    }

    collide(player) {

    }

    sweep() {
        
    }
}
